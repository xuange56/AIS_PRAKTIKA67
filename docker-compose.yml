services:
  backend:
    build: ./backend
    container_name: ep-backend
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: ep-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend

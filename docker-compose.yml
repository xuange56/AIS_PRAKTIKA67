services:
  backend:
    build: ./backend
    container_name: tasks_backend
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: tasks_frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
